% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binLimits.R
\name{binLimits}
\alias{binLimits}
\title{Compute Percentile-Based Bin Limits for Each Variable}
\usage{
binLimits(data, b)
}
\arguments{
\item{data}{A data frame or list where each element (column) is a numeric vector to be binned.}

\item{b}{An integer specifying the number of bins (must be >= 1).}
}
\value{
A list of numeric vectors. Each vector contains \code{b + 1} bin edges for the corresponding column in \code{data}.
The first edge is \code{-Inf} and the last is \code{Inf}, ensuring all values fall into some bin. Values must be real numbers.
}
\description{
This function divides each variable (column) of a numeric data frame into \code{b} bins based on percentile cutoffs.
It returns a list of bin edge vectors, one for each column, where the first and last edges are set to \code{-Inf} and \code{Inf}, respectively, to cover all values.
}
\examples{
df <- data.frame(a = rnorm(100), b = runif(100))
binLimits(df, b = 4)

}
